trigger('pageAnimations', [
  transition(':enter', [
    query(':self, .logo > *, .viking', [
      style({ opacity: 0 }),
      stagger(100, [
        animate('300ms ease-out', style({ opacity: 1 }))
      ])
    ])
  ])
])
