import { ToolTipHoverDirective } from './tool-tip-hover.directive';

describe('ToolTipHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ToolTipHoverDirective();
    expect(directive).toBeTruthy();
  });
});
