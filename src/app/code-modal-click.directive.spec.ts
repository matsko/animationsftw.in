import { CodeModalClickDirective } from './code-modal-click.directive';

describe('CodeModalClickDirective', () => {
  it('should create an instance', () => {
    const directive = new CodeModalClickDirective();
    expect(directive).toBeTruthy();
  });
});
